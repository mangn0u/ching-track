"""Goals URL patterns."""

from django.urls import path

# TODO Day 6: import views
# from .views import (
#     GoalListCreateView, GoalDetailView,
#     GoalContributeView, GoalContributionListView,
# )

urlpatterns = [
    # path("", GoalListCreateView.as_view(), name="goal-list"),
    # path("<int:pk>/", GoalDetailView.as_view(), name="goal-detail"),
    # path("<int:pk>/contribute/", GoalContributeView.as_view(), name="goal-contribute"),
    # path("<int:pk>/contributions/", GoalContributionListView.as_view(), name="goal-contributions"),
]

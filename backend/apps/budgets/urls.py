"""Budget URL patterns."""

from django.urls import path

# TODO Day 4: import views
# from .views import BudgetListCreateView, BudgetDeleteView, BudgetVsActualView

urlpatterns = [
    # path("", BudgetListCreateView.as_view(), name="budget-list"),
    # path("vs-actual/", BudgetVsActualView.as_view(), name="budget-vs-actual"),
    # path("<int:pk>/", BudgetDeleteView.as_view(), name="budget-delete"),
]
